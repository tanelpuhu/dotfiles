#!/bin/bash
? () {
  echo "$*" | bc -l;
} # $ ? 1337 - 1295   => 42

blame(){
  svn blame $@ | less
}

svnlog(){
  svn log -vl 50 $@ | less
}

svndi(){
  svn di $@ | colordiff | less -R
}

changset(){
  svn log --diff -vr $@ | colordiff | less -R
}

lint(){
  pylint $@ | less
}

ttail(){
  tail -f $@ | while read; do echo -e "$(date +%T.%N) $REPLY"; done
}

mktar() {
  tar cvf  "${1%%/}.tar" "${1%%/}/";
}

mktgz() {
  tar cvzf "${1%%/}.tgz" "${1%%/}/";
}
